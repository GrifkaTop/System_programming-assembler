
int mas[100];
int count[10];
for (auto i : mas) {
    count = digit(i);
}

int max_count, val_digit;
for (int i = 0; i < 10; i++) {
    if (max count < count[i]) {
        max count = count [i]; 
        val_digit = i;
    }
}
cout << val_digit;